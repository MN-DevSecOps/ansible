-name : demo with variables
  hosts : localhost
  connection : local
  vars :
    COURSE : "Ansible"
    TRAINER : "MN"
    DURATION : "2 months"
  tasks :
    -debug :
       msg : "hello  iam learning {{ COURSE }} with {{ TRAINER }} for {{ DURATION }}"