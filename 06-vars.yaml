- name: demo on vars variable
  hosts: local
  connection: local # it will not ask for username and password
  vars_files:
  - course.yaml
  tasks:
  - name: print variable information
    debug:
      msg: "Hi, I am learning {{ COURSE }}, Trainer is {{ TRAINER }}, Duration is {{ DURATION }}"